# LoopBack Angular Form Builder and Rendered

The project is generates Forms using form.io builder, presest them in mongoDB and Renderes in Angular1 and Angular2 application

To start the server 
`node .`